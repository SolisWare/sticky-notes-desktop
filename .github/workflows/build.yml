 # Copyright (c) 2026 SolisWare.
 # 
 # All rights reserved. Licensed under the MIT license.
 # See the LICENSE.txt file in the project root directory for details.
name: X-NoTES Build Workflow

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - develop
  workflow_dispatch:

jobs:
  metadata:
    runs-on: ubuntu-latest
    outputs:
      artifact_name_prefix: ${{ steps.resolve.outputs.artifact_name_prefix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve artifact metadata
        id: resolve
        shell: bash
        run: |
          publisher="$(node -p "require('./package.json').author.name.toLowerCase().replace(/[^a-z0-9]+/g, '')")"
          app_name="$(node -p "require('./package.json').name.toLowerCase().replace(/[^a-z0-9]+/g, '')")"
          app_version="$(node -p "require('./package.json').version")"
          release_codename="$(node -p "(require('./app-version-config.json').releaseCodename || '').toLowerCase().replace(/[^a-z0-9]+/g, '-')")"
          echo "artifact_name_prefix=${publisher}-${app_name}-v${app_version}-b${GITHUB_RUN_NUMBER}-${release_codename}" >> "$GITHUB_OUTPUT"

  build:
    needs: metadata
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build
        run: npm run build
        env:
          CI: false

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: build/
          retention-days: 1

  dist-mac-arm64:
    needs: [metadata, build]
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build
          path: build/

      - name: Distribute macOS arm64
        run: npm run dist-mac-arm64 -- --publish never

      - name: Upload macOS arm64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.metadata.outputs.artifact_name_prefix }}-macos-arm64
          path: dist/*arm64.dmg
          retention-days: 30

  dist-mac-x64:
    needs: [metadata, build]
    runs-on: macos-15-intel
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build
          path: build/

      - name: Distribute macOS x64
        run: npm run dist-mac-x64 -- --publish never

      - name: Upload macOS x64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.metadata.outputs.artifact_name_prefix }}-macos-x64
          path: dist/*x64.dmg
          retention-days: 30

  dist-windows:
    needs: [metadata, build]
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build
          path: build/

      - name: Distribute Windows
        run: npm run dist-windows -- --publish never

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.metadata.outputs.artifact_name_prefix }}-windows-x64
          path: dist/*.exe
          retention-days: 30
